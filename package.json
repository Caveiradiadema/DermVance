{
  "name": "dermvance-site",
  "version": "1.0.0",
  "description": "Website para DermVance, especialistas em cicatrização.",
  "scripts": {
    "minify-css": "npx cleancss -o css/styles.min.css css/styles.css",
    "optimize-images": "npx sharp-cli -i \"img-src/**/*.{png,jpg,jpeg}\" -o \"img/\" -f webp -q 80 --resize 1440 1440 --without-enlargement"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "sharp-cli": "^5.2.1"
  }
}